
const fetch = require('node-fetch'); // Assuming node env or just run in browser console context if possible, but for run_command I need node. 
// actually I'll use curl via run_command, it's easier.
